const btnYear = document.querySelector("#btnYear").addEventListener("click", function(e){
        e.preventDefault();
})

const btnBudget = document.querySelector("#btnBudget").addEventListener("click", function(e){
    e.preventDefault();
})

const btnStatus = document.querySelector("#btnStatus").addEventListener("click", function(e){
    e.preventDefault();
})
